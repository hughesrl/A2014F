<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked. -->
    <CheckBoxPreference
        android:key="notifications_new_message1"
        android:title="Email New Messages"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message2"
        android:title="Email Interest in me"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message3"
        android:title="New matches"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message4"
        android:title="Email Offers and Promo"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message5"
        android:title="New messages"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message6"
        android:title="Interest in me"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message7"
        android:title="Someone viewed my profile"
        android:defaultValue="true" />
    <CheckBoxPreference
        android:key="notifications_new_message8"
        android:title="Someone added me as a favorite"
        android:defaultValue="true" />

    <!-- Allows the user to choose a ringtone in the 'notification' category. -->
    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
    <!--<RingtonePreference
        android:dependency="notifications_new_message"
        android:key="notifications_new_message_ringtone"
        android:title="@string/pref_title_ringtone"
        android:ringtoneType="notification"
        android:defaultValue="content://settings/system/notification_sound" />-->

    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!--<CheckBoxPreference
        android:dependency="notifications_new_message"
        android:key="notifications_new_message_vibrate"
        android:title="@string/pref_title_vibrate"
        android:defaultValue="true" />-->

</PreferenceScreen>
